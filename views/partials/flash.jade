#flash
  if messages.errors
    .alert.alert-danger.animated.fadeIn.alert-dismissable
      button.close(type='button', data-dismiss='alert', aria-hidden='true') ×
      for error in messages.errors
        div= error.msg
  if messages.info
    .alert.alert-info.animated.fadeIn.alert-dismissable
      button.close(type='button', data-dismiss='alert', aria-hidden='true') ×
      for info in messages.info
        div= info.msg
  if messages.success
    .alert.alert-success.animated.fadeIn.alert-dismissable
      button.close(type='button', data-dismiss='alert', aria-hidden='true') ×
      for success in messages.success
        div= success.msg